#!/usr/bin/env node
var debug = require('debug')('my-application');
var app = require('../app');

//app.set('port', process.env.PORT || 3000);
/*
var server = app.listen(app.has('port') ? app.get('port') : 80, function() {
  console.log('Express server listening on port ' + server.address().port);
});
*/
